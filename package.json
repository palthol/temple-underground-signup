{
  "name": "temple-underground-signup",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "services/*"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:*",
    "dev:waiver": "npm --workspace apps/waiver run dev",
    "dev:api": "npm --workspace services/api run dev",
    "build": "npm-run-all build:*",
    "build:waiver": "npm --workspace apps/waiver run build",
    "start": "npm --workspace services/api run start",
    "typecheck": "echo 'no types yet'",
    "lint": "echo 'no linter yet'"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "@uiw/react-signature": "^1.3.2"
  }
}
